#include <stdio.h>

// Define a macro swap(t, x, y) that interchanges two arguments of type t
// Seems like I use #define and then write code?
// Need to get this done tomorrow :)

int main(void)
{
    
}